import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartenergymonitor.app',
  appName: 'SMART ENERGY MONITOR ',
  webDir: 'dist'
};

export default config;
